# Active changes

Put in-progress OpenSpec changes here as `openspec/changes/<kebab-id>/`.

When done: merge deltas into `openspec/specs/`, then move the folder to `archive/YYYY-MM-DD-<id>/`.

No active changes at bootstrap.
